const { assert } = require("chai");

var add = (x, y) => x + y;

describe('course test suite', () => {
    it('should return 2', () => {
        assert.equal(add(2, 2), 4);
    });

    it('should return 1', () => {

    });
    it('should return 0', () => {

    });
    it('should return -1', () => {

    });
})